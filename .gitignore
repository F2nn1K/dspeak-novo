# Dependências
node_modules/

# Dados de execução (gerados pelo servidor — nunca commitar)
channels.json
servers.json
roles.json
messages.json
dm-messages.json
users.json
uploads/
music.json
push.json
friends.json
server.log

# Segredos guardados na VPS
.secrets/

# Instalador do app desktop (106MB — sobe pra VPS via scp, não pelo git)
downloads/

# Ambiente / segredos
.env
.env.*
key.txt
aic-cache/

# Sistema operacional
Thumbs.db
.DS_Store
